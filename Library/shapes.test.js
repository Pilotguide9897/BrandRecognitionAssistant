const Shapes = require('./shapes.js')

describe('Shapes',() => {  // Generating the test suite.


    describe( 'circle', () => { // Creating my test instance.
        it ('should...', () => {
            expect(shape.render()).toEqual('<polygon points = "150, 18 244, 182 56, 182" fill="blue"/>');
        })
    })

    describe( 'square', () => { // Creating my test instance.
        it ('should...', () => {
            expect(shape.render()).toEqual('<polygon points =
        })
    })

    describe( 'triangle', () => { // Creating my test instance.
        it ('should...', () => {
            expect(shape.render()).toEqual('<polygon points =
        })
    })


})
